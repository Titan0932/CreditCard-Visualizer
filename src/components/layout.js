export const Layout = ({ children }) => {
  return (
    <div className=''>
      <div className="container container-wrapper mt-5 bg-light rounded px-3 pt-0 pb-3">{children}</div>
    </div>
  );
};
